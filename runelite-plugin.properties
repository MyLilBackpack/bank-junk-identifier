# RuneLite Plugin Hub manifest.
# Placed at the ROOT of the plugin's own GitHub repo (NOT the plugin-hub repo).
# See SHIP_CHECKLIST.md for the full submission steps.

displayName=Dumb Old Man
author=REPLACE_WITH_YOUR_GITHUB_HANDLE
description=Highlights quest items and other bank clutter you no longer need, with smart upgrade awareness.
tags=bank,quest,items,cleanup,junk,stash,clue,holiday,poh
# NOTE: this MUST be the fully-qualified main class of the *shipped* package.
# The working tree currently uses com.bankjunk, which is
# the RuneLite core namespace and is not appropriate for a hub plugin — rename the
# package (e.g. com.bankjunk) before submitting and update this line to match.
plugins=com.bankjunk.BankJunkPlugin
version=1.0.0
build=standard
